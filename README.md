# Captain Supply Analysis | Rapido Assessment

This submission answers two supply questions for Rapido:

- **Part A:** where the captain onboarding funnel loses people, which fixes are practical, and whether WA002 should be scaled.
- **Part B:** when the airport marketplace is short, whether the trip data points to a return-utilisation problem, and whether airport acquisition should start now.

## Start here

For a quick review, start with the executive memo or presentation. The notebooks contain the detailed calculations and saved results.

| Resource | What to expect |
| --- | --- |
| [Executive memo](deliverables/memo.pdf) | Two-page decision summary, priorities, assumptions, and rollout criteria |
| [Presentation](deliverables/presentation.pdf) | Six-slide overview of findings and proposed actions |
| [Part A notebook](Part-A_analysis.ipynb) | Onboarding funnel, campaign evaluation, and recovery scenarios |
| [Part B notebook](Part-B_analysis.ipynb) | Airport shortage, trip outcomes, and pilot sizing |
| [Dashboard code](app.py) | Interactive results after generating the notebook exports |

## Key findings

| Area | Finding | Recommendation |
| --- | --- | --- |
| Onboarding | 3,532 of 20,207 mature signups approved within 30 days: **17.5%**. RC has the largest document-stage loss. | Test Insurance upload recovery and guided document recapture. |
| Recovery scenarios | Approximately **175 additional approvals/month** from Insurance recovery and **71/month** from image-quality repair under the base assumptions. | Validate recovery and downstream conversion through randomized pilots. |
| WA002 | **+4.2 percentage-point adjusted association**, with a 95% bootstrap interval of **+2.7 to +5.8 points**. | Run an RC-stage randomized test before approving a blanket 5Ã— spending increase. |
| Airport supply | **31.9% fulfilment** during 21:00â€“04:00, with approximately **754 unfulfilled requests/day** across the terminals. | Test timed availability and return support before expanding acquisition. |

Recovery estimates are scenarios, not proven incremental gains. The campaign comparison does not establish causality.

## Repository contents

The project includes:

```text
Part-A_analysis.ipynb       Part A working, explained cell by cell
Part-B_analysis.ipynb       Part B working, explained cell by cell
app.py                      Streamlit dashboard
utils.py                    Shared loading, validation, and calculations
README.md                   This guide
Data/                       Original CSV inputs
outputs/                    Generated locally by the notebooks
deliverables/
â”œâ”€â”€ memo.pdf                Two-page executive decision memo
â””â”€â”€ presentation.pdf        Six-slide presentation deck
```

The raw `Data/` folder must stay beside these files when rerunning the analysis. It contains the original CSV inputs and is never overwritten. The `outputs/` folder is generated by the notebooks and supplies the tables and charts read by the dashboard.

## How the project works

The notebooks are the analytical working. They read the raw CSVs, validate them through `utils.py`, calculate the results, save tables and charts under `outputs/`, and record run metadata. The memo and presentation in `deliverables/` are supplied as static PDFs; the notebooks do not regenerate them. Update the PDFs separately if the findings change.

The dashboard does not execute the notebooks. It reads the validated notebook exports through `utils.py`, checks the raw-file hashes, and presents the same results in two tabs. This keeps the dashboard fast and prevents it from silently showing results produced from different CSVs.

1. The notebooks load and validate `Data/*.csv` through `utils.py`.
2. They export tables, charts, summaries, and run manifests to `outputs/part_a/` and `outputs/part_b/`.
3. The dashboard reads the exports and checks the recorded input fingerprints against the current CSVs.

Generate the exports before launching the dashboard. Rerun the notebooks after changing the data or analysis code.

## Setup

Python 3.12 is recommended. The following commands use a Linux/macOS shell. From the project folder:

```bash
python3 -m venv .venv
.venv/bin/python -m pip install pandas numpy scipy scikit-learn matplotlib seaborn nbformat nbclient ipykernel nbconvert streamlit threadpoolctl
```

In VS Code, open each notebook and select `.venv/bin/python` as the notebook kernel.

## Run the analysis

Run both notebooks from top to bottom, in this order:

1. Open `Part-A_analysis.ipynb` and choose **Run All**.
2. Open `Part-B_analysis.ipynb` and choose **Run All**.

The notebooks are also executable from the project directory:

```bash
.venv/bin/python -m jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.timeout=-1 Part-A_analysis.ipynb
.venv/bin/python -m jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.timeout=-1 Part-B_analysis.ipynb
```

The terminal commands update the notebooks in place. The timeout setting allows long-running cells to finish. Dependencies are not pinned in the installation command; the run manifests record selected package versions from each execution.

Part A includes model sensitivity checks and a 300-resample campaign bootstrap, so it takes longer than a simple CSV summary. Part B uses 2,000 calendar-day bootstrap resamples. The notebooks record input hashes, package versions, seeds, and cohort/cutoff metadata in their run manifests. Detailed analytical rules are documented in the code and notebooks.

## Run the dashboard

Run the notebooks first, then start Streamlit:

```bash
.venv/bin/python -m streamlit run app.py --server.address 127.0.0.1
```

Open `http://localhost:8501`. The dashboard has two tabs:

- **Onboarding:** funnel, segment comparisons, document-quality opportunities, WA002 evidence, recommendations, data-quality notes, and downloads for the memo and presentation.
- **Airport supply:** hourly shortage, month validation, trip outcomes, pilot sizing, and data-quality notes.

## Analysis definitions and assumptions

- **Onboarding outcome:** approval and first order within 30 elapsed days of signup. The main cohort is Januaryâ€“May signups because June does not have a complete 30-day follow-up window.
- **Document funnel:** stages are reconstructed from event timestamps. A captain's first unpassed required document owns the loss. ERickshaw skips Permit.
- **WA002:** recipients and controls are aligned after the Registration Certificate step. The reported adjusted difference is an association, not proof that the message caused the improvement. A randomized test at that stage is required before scale-up.
- **Airport marketplace:** the hourly file is used for demand and fulfilment. The trip file is a separate sample and is not multiplied into marketplace totals.
- **Trip denominators:** cancellation rates use the full trip sample. Return-fare rates use non-cancelled records with a 24-hour end buffer. Standardization coverage is the proportion of eligible sample rows in supported groups.
- **Airport pilot:** the 20-slot, 28-night scenario assumes 80% attendance, 0.75 completed requests per effective captain-hour, INR 30 per available hour, and a bounded INR 60 fallback. The fallback is capped at 20 payments per night. The 28-night experiment includes 14 treatment nights; the separate 30-day operating-cost scenario is not the experiment budget. These are planning assumptions, not a permanent business case.
- **Costs:** illustrative variable costs exclude fixed engineering, airport fees, acquisition bonuses, and captain earnings.

## Decision in one paragraph

Fix onboarding completion before buying more reach: start an Insurance handoff pilot and guided document recapture, while holding blanket WA002 scaling. At the airport, run a timed night-supply and return-utilisation pilot before funding a catchment signup bonus. The decision should change only after randomized results show incremental approvals or fulfilments, acceptable quality and complaints, stronger return utilisation, and Finance-approved unit economics.

## Known data limits

The onboarding files contain summary/event mismatches and clicked-but-undelivered messages; those are reported instead of silently corrected. The airport hourly data do not include flight schedules, queue position, dispatch events, unique captains, completion timestamps, or earnings. The trip sample is not a validated subset of hourly completed trips, so it supports a directional operational hypothesis rather than an acquisition ROI claim.
